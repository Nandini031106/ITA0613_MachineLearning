# Climate-Resilient Crop Yield Forecasting

Educational implementation for the ML assignment.

## Run
```bash
pip install -r requirements.txt
python main.py
```

Outputs are saved under `results/`.

## Important
The included dataset is generated locally for demonstration and reproducible screenshots. For the final submission, replace it with the public agro-climatic dataset required by your assignment and rerun the pipeline.
